# Console Administrativo do Control Plane (ADR-024).
# API de operador no domínio CENTRAL (sem tenancy/subdomínio).

# Dev no host (nginx do stack = :8090):
VITE_API_URL=http://localhost:8090

# Dev em container (rede interna): VITE_API_URL=http://nginx:80
